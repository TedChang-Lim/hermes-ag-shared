# 🕵️ 큐리(Quri) → 해나 · 작업 능력 및 방법 기록

> 마스터님 지시로 작성. 해나가 읽고 큐리의 작업 방식을 파악·인계받을 수 있도록, **실제로 수행해 검증된 것만** 적었습니다. 추측·과장 없음(팀 규칙 준수).

---

## 작업 요약
큐리(Qoder, 5번째 에이전트)가 새미새론AI교육원 프로젝트에서 실제로 수행·검증한 핵심 작업법 3종을 공유합니다.
1. **로고 합성 파이프라인** (원본 글자를 깨지 않고 잠금 로고 제작) — ✅ 이번 세션에서 직접 수행·검증
2. **영상 일관성 유지** (생성 클립의 마지막 프레임 발췌 → 다음 컷 참조) — ✅ 이전 세션에서 수행
3. **HWP/HWPX 문서 처리** — ⚠️ 아래 정직 참고사항 필독

---

## 🎯 큐리 정체성 (참고)
- 코드와 이야기를 모두 다루는 탐정 컨셉. 버그의 범인을 잡고 시나리오 다음 장면을 설계.
- 마스터님은 **비개발자** → 기술 용어·숫자 대신 **시각 결과물 + 번호 선택지**로 소통하는 게 핵심 원칙.

---

## 1. 로고 합성 파이프라인 (검증 완료)

### 문제
AI 이미지 생성(ImageGen)으로 로고를 "다시 그리면" 한글 글자(특히 `ㄹ`)가 매번 깨지고, 의도한 디자인이 변형됨.

### 해결 원칙
> **다시 그리지 말고, 원본을 추출해서 합성한다.** (regenerate ❌ / extract + composite ✅)

### 워크플로우 (Python + Pillow + NumPy)

| 단계 | 방법 | 핵심 값 |
|------|------|---------|
| **1. 원본 워드마크 추출** | 소스 PNG에서 로고 영역 크롭 → 배경색(크림)과의 픽셀 거리로 알파 마스크 생성 → 희미한 그리드선 제거 후 투명배경 PNG로 저장 | 크롭 `(80,250,820,463)`, 알파 램프 `70→108` |
| **2. 글자 본문 기준선 측정** | 알파 채널의 **왼쪽 45%** 만 분석(오른쪽 상승 곡선·주황 점 제외) → 진짜 글자 상단/하단(베이스라인) 산출 | `np.where(alpha[:, :WW*0.45].max(axis=1) > 40)` |
| **3. 서브텍스트 렌더** | `AppleSDGothicNeo.ttc`(index 4)로 "AI 교육원"·"SEMISERON"을 자간(tracking) 적용해 한 글자씩 그린 뒤, 실제 잉크 경계(`getbbox`)로 크롭 | KR 56px / EN 30px, 자간 KR 4 / EN 10 |
| **4. 잠금(lockup) 합성** | 가로형 = 워드마크 + 세로 구분선 + [AI교육원/SEMISERON] 덩어리. 세로형 = 워드마크 아래 덩어리 | 위치는 파라미터로 제어 |
| **5. 반복 산출** | 매 실행마다 **타임스탬프 파일명**으로 저장 → 이미지 캐시 무력화 | `name_{int(time.time())}.png` |

### ⚠️ 시행착오에서 얻은 교훈 (해나 필독)
1. **이미지 캐시 함정**: 같은 파일명을 덮어쓰면 사용자의 뷰어가 **이전 이미지를 캐시로 계속 보여줌** → "아무리 고쳐도 안 변한다"는 항의의 진짜 범인. **반드시 매번 새 파일명**으로 저장.
2. **캔버스 높이**: 덩어리를 아래로 내리면 캔버스 높이도 함께 키워야 잘리지 않음 (`ch = max(기본, 덩어리밑변+여백)`).
3. **비개발자 소통**: "숫자로 미세조정하세요"는 금지. **위치 옵션 3개를 번호 붙인 비교 이미지**로 만들어 "가로 3번, 세로 2번"처럼 번호로 선택하게 함.
4. **인쇄용 한계**: 추출한 워드마크는 래스터(678×188px)라 크게 키우면 번짐. 현수막·간판용은 **벡터(SVG) 재제작** 필요 — 이 부분은 아직 미수행.

### 산출물 위치
- 합성 스크립트: `~/Documents/Qoder/2026-07-25/chat-1/build_lockup.py`, `build_options.py`
- 포토샵용 투명 조각: `~/Documents/Qoder/2026-07-25/chat-1/logo_pieces/` (로고·AI교육원·SEMISERON·구분선 4종)

---

## 2. 영상 일관성 유지 — 마지막 프레임 발췌 (검증 완료)

### 목적
여러 컷을 연속 생성할 때, 앞 컷의 **마지막 프레임**을 다음 컷의 시작 참조로 써서 화면이 끊기지 않게 유지.

### 방법
1. 생성된 클립을 받으면 **맨 마지막 프레임**을 추출(캡처).
2. 그 프레임을 마스터님께 보여주고, 다음 컷 생성의 참조 이미지로 사용.
3. 캐릭터 시트는 정면 1장이 아니라 **얼굴 클로즈업 + 정면 전신 + 측면** 등 다각도를 16:9 한 장에 담아 일관성 앵커로 활용(nanobanana2 선호).

### 노트
- Seedance 2.0은 대사를 음성으로 생성하지 않음 → 대사/나레이션은 편집 단계에서 별도 삽입.
- "무제한 생성(Unlimited)"은 **웹 UI에서만** 가능. MCP/CLI 직접 생성은 불가.

---

## 3. HWP/HWPX 문서 처리 — ⚠️ 정직한 상태 보고

> **추측·거짓 금지 원칙에 따라 솔직히 적습니다.**

- 큐리 세션 기록에는 **큐리가 직접 HWP를 읽어 HWPX를 재구성한 작업 이력이 확인되지 않습니다.**
- HWP 양식 자동화(`hwp-form-automation`, kordoc MCP 연동)는 **해나 본인의 스킬**로 보입니다(2026-07-22 해나 작업완료 보고 참조).
- 따라서 이 항목은 "큐리가 했다"고 주장하지 않습니다. 마스터님이 큐리에게도 해당 작업을 맡기셨다면 **별도 세션에서 수행 후 검증 결과를 다시 기록**하겠습니다.

### 큐리가 이해하고 있는 HWPX 처리 일반 원칙 (참고용)
- `fill_form` + `hwpx-preserve` 모드로 **원본 서식을 건드리지 않고 텍스트만 치환**.
- HWP 바이너리 직접 출력은 불가 → **HWPX로 출력** 후 한컴에서 HWP 변환.
- 표 행 추가/삭제는 가능, **열 구조 변경은 불가**.

---

## ✅ 능력 요약 (가능 / 불가)

| 가능 | 불가·주의 |
|------|-----------|
| ✅ 원본 로고 추출·합성 (글자 보존) | ❌ AI 재생성으로 원본 글자 완벽 재현 |
| ✅ 투명배경 조각 분리 (포토샵 인계용) | ⚠️ 래스터 로고의 무한 확대 (벡터 재제작 필요) |
| ✅ 영상 마지막 프레임 발췌·일관성 유지 | ❌ Seedance 음성 대사 생성 |
| ✅ 비개발자용 시각 선택지 소통 | ❌ MCP/CLI 무제한 영상 생성 (웹 UI 전용) |
| ✅ Python/Pillow·NumPy 이미지 처리 | ⚠️ HWP 직접 처리 — 이번 세션 미검증 |

---

## 🚀 다음 액션 제안 (해나 협업용)
- 로고 위치가 마스터님 포토샵에서 확정되면 → **벡터(SVG) 재제작** 협업 가능.
- 이후 순서: ① 로고 확정 → ② 홈페이지 히어로 배경영상(10~15초 무음 루프) → ③ 홍보영상(30초~1분).

---

**작성일**: 2026-07-31
**작성자**: 큐리 (Qoder Agent, 5번째)
**상태**: 로고·영상 방법 검증 완료 / HWP 항목은 정직 보류
