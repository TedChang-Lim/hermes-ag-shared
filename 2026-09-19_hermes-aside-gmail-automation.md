# [DaMoA Wiki] Hermes 에이전트 + Aside 브라우저 자동화 — Gmail 관제 자동화와 자격증명 정책

작성: 해나 (Hermes Agent) / 2026-09-19
이전 문서: `2026-08-15_aside-browser-integration.md` (도입), `2026-08-16_aside-browser-ide-hybrid-analysis.md` (분석)
이 문서는 그 뒤 **실제로 돌아가게 만든 결과**와, 그 과정에서 걸린 것들을 기록한다.

---

## 1. 한 줄 요약

해나(Hermes Agent)가 Aside 로 마스터님 크롬 프로필에 직접 들어가 Gmail 을 읽고 정리한다.
구글 OAuth 를 쓰지 않으므로 7일 만료 문제가 없다.
2026-09-19 부터 **매일 22:00 텔레그램 보고**로 자동 운영된다.

---

## 2. 전체 구조

```
마스터님 (사람)
   │  텔레그램 / Hermes 데스크톱 앱
   ▼
해나 = Hermes Agent   (모델: opencode-go 경유 deepseek-v4.1-flash)
   │
   ├─ 크론 스케줄러 ── 매일 22:00 자동 실행
   ├─ 스킬 / 메모리
   └─ MCP 도구
        ├─ aside              → Aside 데몬 → 마스터님 크롬 프로필 (로그인 유지)
        ├─ open-design        → 디자인 산출물
        ├─ kordoc             → HWP / HWPX / PDF
        ├─ codegraph          → 코드 탐색
        └─ higgsfield / higgsfield-bridge → 이미지·영상 생성
```

**핵심 발상**: Aside 는 "이미 로그인된 브라우저"를 도구로 바꿔준다.
해나에게 비밀번호를 주지 않아도, 마스터님이 로그인해 둔 세션을 그대로 쓴다.

---

## 3. 왜 OAuth 를 버렸나

| 항목 | 구글 OAuth (gws) | Aside 브라우저 세션 |
|---|---|---|
| 인증 유지 | 동의화면이 "테스트" 상태면 **7일마다 만료** | 마스터님이 직접 로그인한 세션 = 만료 없음 |
| 만료되면 | 크론잡이 조용히 차단 | 없음 |
| 사전 심사 | 구글 검증 심사 통과해야 프로덕션 승격 | 불필요 |
| 다룰 수 있는 범위 | Gmail·캘린더·드라이브 API 가 정한 범위 | 사람이 화면에서 하는 일 전부 |
| 비용 | 0원 | 0원 (무료 플랜) |

실측 기록:
- 프로젝트 `hermeslink-500307` / OAuth 클라이언트 2026-07-24 생성 → **2026-08-08 토큰 만료**
- 그 뒤 Gmail 크론잡이 **2026-08-11 ~ 09-18, 39회 연속 차단**
- 차단 사유: `missing credential file google_token.json`

---

## 4. 실제로 돌아가는 것 — 매일 22:00 Gmail 점검

크론잡 id `3f60f4f4b85a` / 매일 22:00 / 텔레그램 전송

### 3단 분리 구조

| 단계 | 담당 | 파일 |
|---|---|---|
| 수집 | 셸 스크립트 (모델을 쓰지 않음) | `~/.hermes/scripts/gmail-unread.sh` + `.js` |
| 판단·보고 | 해나 (모델) | `~/.hermes/scripts/gmail-check-prompt.md` |
| 삭제 | 마스터님 승인 후에만 | `~/.hermes/scripts/gmail-trash.sh` + `.js` |

**수집을 스크립트로 뺀 이유**
매일 같은 동작을 모델이 새로 짜면 토큰도 쓰고 틀리기도 한다.
모델은 "무엇이 중요한가"만 판단하게 하고, 화면에서 긁어오는 일은 코드가 맡는다.
수집 소요 11초.

### 삭제는 2단계

```
gmail-trash.sh "from:quora.com"              → 모의실행 (개수만 셈, 안 지움)
gmail-trash.sh --execute "from:quora.com"    → 실제 휴지통 이동
```

기본값이 모의실행이라 실수로 지울 수 없다.

### 2026-09-19 02:51 첫 실행 결과 (실측)

- 안 읽은 메일 **26건**
- **중요 16건** — Cloudflare 확인 2, OpenRouter 부가세 안내, 구글 보안 알림 4, Unity ID 2, SpaceXAI 로그인, DeepSeek V4 Pro 안내, 본인 메모, Google One·Play 요금 안내 4
- **정리 후보 10건** — Quora Digest 6, 마케팅·공지 4
- 텔레그램 전송 성공 / 스케줄러 상태 정상

---

## 5. 설정이 어려웠던 지점 (전부 실측)

막혔다가 넘긴 것들. 강의·책에서 그대로 쓸 수 있는 부분.

1. **stdin 파이핑 금지**
   `echo "코드" | aside repl` 은 BrokenPipe 로 죽는다.
   코드 전체를 인자로 넘겨 1회 호출해야 한다.

2. **탭은 호출 간에 유지되지 않는다**
   `openTab` → 추출 → `closeTab` 까지 한 번의 호출 안에서 끝내야 한다.
   중간에 나눠 쓰면 다음 호출에서 탭을 못 찾는다.

3. **데몬이 꺼져 있으면 전부 실패**
   `open -a Aside` 로 켜고 10~30초 기다려야 한다.
   재부팅 후를 대비해 로그인 항목에 등록해 두었다.

4. **Cloudflare 로 네임서버를 옮길 때는 IP 도 같이 바꿔야 한다**
   도메인클럽은 네임서버 이름과 IP 가 함께 맞지 않으면 신청을 거부한다.

5. **CDN·로컬 DNS 캐시**
   변경 후에도 옛 IP 로 붙어 "안 되는 것처럼" 보인다.
   검증할 때는 `--resolve` 로 직접 찍거나 별도 리졸버를 써야 한다.

6. **공개 저장소에 비밀값** — 가장 아팠던 것. 6장 참조.

---

## 6. 자격증명 취급 원칙

**이 저장소는 공개(public)다. 값을 적지 않는다.**

2026-09-19 발견·조치:
- `2026-08-15_aside-browser-integration.md` 에 Aside 마스터 비밀번호와 12단어 복구 키가 평문으로 기재 (8/15 부터 한 달간 공개 상태)
- MiMo API 키가 코드 3개 파일에 하드코딩
- 전부 제거 후 커밋·푸시. 깃허브 API 검색 결과 0건 확인
- **다만 커밋 이력에는 남는다 → 값 자체를 교체해야 끝난다**

앞으로의 규칙:
- 값은 `~/.hermes/credentials/<서비스>.txt` (권한 600) 에만 둔다
- 코드는 그 파일을 읽는다. 소스에 하드코딩하지 않는다
- 저장소에는 **값 없는 목록표**만 올린다
- 웹 로그인 자동 입력은 Hermes vault 를 쓴다

---

## 7. 책·강의로 쓸 때의 뼈대

### 이야기 순서

1. **문제** — AI 에이전트에게 메일을 맡기고 싶다
2. **첫 시도** — 구글 OAuth. 됐다. 그런데 일주일 뒤 조용히 죽는다
3. **발견** — 구글은 "테스트" 상태 앱의 토큰을 7일 뒤 버린다. 심사를 통과해야 안 버린다
4. **전환** — 비밀번호를 주지 말고, 이미 로그인된 브라우저를 쓰게 하자
5. **도구** — Aside. 마스터님이 평소 쓰는 크롬을 그대로 자동화 대상으로 삼는다
6. **구조** — 수집은 스크립트, 판단은 모델, 삭제는 승인
7. **사고** — 공개 저장소에 적어둔 비밀번호. 편의와 보안은 같이 설계해야 한다
8. **결론** — 자동화의 병목은 모델 성능이 아니라 인증이 얼마나 오래 살아 있느냐다

### 강의에서 강조할 한 줄

> 자동화가 멈추는 이유는 대개 똑똑하지 않아서가 아니라, 로그인이 풀려서다.

### 강의 실습으로 바꾸려면

- 1교시: Aside 설치·로그인, `aside repl` 한 줄로 페이지 제목 긁어오기
- 2교시: Gmail 목록 긁어 JSON 으로 만들기
- 3교시: 그 JSON 을 AI 에게 주고 "중요한 것만 골라줘" 시키기
- 4교시: 크론으로 매일 돌리기 + 텔레그램 알림
- 5교시: 자격증명을 왜 저장소에 적으면 안 되는가
