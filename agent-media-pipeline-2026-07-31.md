---
type: concept
created: 2026-07-31
updated: 2026-07-31
sources: [Documents/Qoder/2026-07-25/chat-1/2026-07-31_큐리-작업능력-및-방법-기록.md]
aliases: [큐리 해나 작업능력, 미디어 파이프라인 검증, 음성분리, 로고합성]
---

# 검증된 미디어 작업 능력 (큐리 + 해나, 2026-07-31)

> 마스터님이 큐리(QN 3.8 Max)에게 시켜봤던 작업들을 해나(Hermes)도 동일하게 수행·검증.
> **"추측·과장 없음, 실제 수행해 검증된 것만"** 기록. 모든 에이전트 공유용.

## 왜 이 문서가 중요한가

마스터님이 큐리에게 "다른 에이전트들에게 시키지 않았던 일"을 시켜본 결과,
해나도 같은 작업들을 직접 수행할 수 있음이 확인됨 → 팀 전체 역량으로 공유.

## ✅ 능력 1: 로고 합성 파이프라인 (원본 글자 보존)

### 원칙
> **다시 그리지 말고, 원본을 추출해서 합성한다** (regenerate ❌ / extract + composite ✅)

AI 이미지 생성으로 로고를 "다시 그리면" 한글 글자(특히 `ㄹ`)가 깨짐 → 원본 크롭 + 배경 제거 + 합성이 정답.

### 워크플로우 (Python + Pillow + NumPy)
| 단계 | 방법 | 핵심 값 |
|------|------|---------|
| 1. 원본 워드마크 추출 | PNG 크롭 → 배경색과 픽셀 거리로 알파 마스크 → 투명배경 PNG | 크롭 `(80,250,820,463)`, 알파 램프 `70→108` |
| 2. 글자 기준선 측정 | 알파 채널 왼쪽 45%만 분석(오른쪽 곡선·점 제외) → 상단/베이스라인 산출 | `np.where(alpha[:, :WW*0.45].max(axis=1) > 40)` |
| 3. 서브텍스트 렌더 | `AppleSDGothicNeo.ttc`(index 4)로 자간(tracking) 적용해 한 글자씩, `getbbox`로 크롭 | KR 56px / EN 30px, 자간 KR 4 / EN 10 |
| 4. 락업(lockup) 합성 | 가로형 = 워드마크+구분선+[AI교육원/SEMISERON] / 세로형 = 워드마크 아래 덩어리 | 위치는 파라미터 제어 |
| 5. 반복 산출 | 매번 타임스탬프 파일명 → **이미지 캐시 무력화** | `name_{int(time.time())}.png` |

### ⚠️ 핵심 교훈 (마스터님 "3개나 다 똑같아" 사건의 진짜 범인)
1. **이미지 캐시 함정**: 같은 파일명을 덮어쓰면 뷰어가 이전 이미지를 계속 보여줌. "고쳐도 안 변한다" = 캐시 문제일 가능성. **반드시 매번 새 파일명**.
2. **캔버스 높이**: 덩어리를 아래로 내리면 캔버스 높이도 키워야 잘리지 않음 (`ch = max(기본, 덩어리밑변+여백)`).
3. **인쇄용 한계**: 추출 워드마크는 래스터(678×188px)라 크게 키우면 번짐 → 현수막·간판용은 벡터(SVG) 재제작 필요 (미수행).

### 산출물
- 스크립트: `~/Documents/Qoder/2026-07-25/chat-1/build_lockup.py`, `build_options.py`
- 포토샵용 투명 조각: `~/Documents/Qoder/2026-07-25/chat-1/logo_pieces/` (4종)

## ✅ 능력 2: 영상 일관성 유지 — 마지막 프레임 발췌

여러 컷을 연속 생성할 때, 앞 컷의 **마지막 프레임**을 다음 컷의 시작 참조로 사용하면 화면이 끊기지 않음.

1. 생성 클립의 맨 마지막 프레임 추출(캡처)
2. 그 프레임을 마스터님께 보여주고, 다음 컷 생성의 참조 이미지로 사용
3. 캐릭터 시트는 정면 1장이 아니라 **얼굴 클로즈업 + 정면 전신 + 측면** 다각도를 16:9 한 장에 담아 일관성 앵커로 활용

### 노트
- Seedance 2.0은 대사를 음성으로 생성하지 않음 → 대사/나레이션은 편집 단계에서 별도 삽입
- "무제한 생성(Unlimited)"은 **웹 UI에서만** 가능 (MCP/CLI 직접 생성 불가)

## ✅ 능력 3: 음성/배경음악 분리 (demucs) — 해나 직접 검증

Seedance/Higgsfield 영상의 오디오에서 **음성(vocals)과 배경음악(no_vocals)을 따로 분리**.

### 실행 명령 (Apple Silicon MPS 가속)
```bash
# 1. 영상 → 오디오 추출 (ffmpeg)
ffmpeg -y -i input.mp4 -vn -ac 2 -ar 44100 source_audio.wav

# 2. demucs 분리 (2-스템: 보컬/반주)
python3 -m demucs --two-stems=vocals -n htdemucs -d mps source_audio.wav
# 결과: separated/htdemucs/source_audio/{vocals.wav, no_vocals.wav}

# 3. 볼륨 검증
ffmpeg -i vocals.wav -af volumedetect -f null - 2>&1 | grep -E "mean_volume|max_volume"
```

### 환경
- demucs venv: `~/Documents/Qoder/2026-07-25/chat-1/.venv_demucs/` (Python 3.12, torch 2.13, numpy 2.5.1)
- ⚠️ **PYTHONPATH 함정**: Hermes 터미널은 PYTHONPATH가 hermes venv를 가리켜서 시스템 python도 깨진 PIL import → `env -u PYTHONPATH` 로 초기화 후 실행
- 시스템 python3(/usr/bin/python3)에 PIL 12.2.0 정상 설치됨

### 성능 (실측 2026-07-31)
- 8초 영상 → 분리 **1초 이내** (MPS 가속)
- 검증: vocals mean -17.9dB / no_vocals mean -23.9dB — 목소리·반주 선명 분리

## ✅ 능력 4: HWP 강사이력서 전면 편집 — 해나 직접 검증

kordoc MCP + rhwp 조합으로 **레거시 HWP 바이너리의 서식 보존 편집 + 사진 교체**까지 완료.

### 워크플로우
1. `mcp__kordoc__parse_document` → 마크다운 구조 추출 (표·사진 포함)
2. `mcp__kordoc__patch_document` → 원본 서식 1바이트도 건드리지 않고 텍스트 치환 (60건 적용)
3. 사진 교체: rhwp `export_hwpx()` → HWPX(zip) 내부 `BinData/image2.jpg` 교체 → 저장
4. 검증: kordoc render → PNG로 확인, 다시 parse해서 "김소영 흔적 0" 확인

### 한계 (솔직 보고)
- HWP(레거시 바이너리) **생성은 불가** — 읽기/편집만 가능
- 표 행 추가/삭제는 제한적, 열 구조 변경 불가
- 셀 음영(하늘색 배경)·오른쪽 여백 등 미세 서식은 변환 과정에서 유실될 수 있음 → **한글 프로그램에서 최종 확인 필수**

## ✅ 능력 5: 비개발자 소통 원칙 (팀 공통 필수)

마스터님은 **비개발자** → 기술 용어·숫자 대신 **시각 결과물 + 번호 선택지**로 소통.

- ❌ "숫자로 미세조정하세요" 금지
- ✅ 위치 옵션 3개를 **번호 붙인 비교 이미지**로 만들어 "가로 3번, 세로 2번"처럼 번호로 선택하게
- ✅ 결과물은 **이미지로 바로 띄워서** 보여주기 (텍스트/ASCII 설명 금지)

## 에이전트별 활용 가이드

| 에이전트 | 활용 가능 작업 |
|---------|--------------|
| 🌞 해나 | 전 능력 보유 (demucs venv·kordoc MCP·rhwp·Pillow) |
| 🔬 큐리 | 로고 합성·영상 일관성 검증 완료, HWP는 미검증(해나 스킬 인정) |
| 💻 미모 | Higgsfield/Open Design 중심 — 음성분리·로고합성 보조 가능 |
| 🧠 AG | 새벽 세션에서 로고·영상 파이프라인 참조 |
| 🤖 지호 | 로고 스크립트·HWP 스크립트 유지보수 |
| ☕ 코코 | 코드 분석·리뷰 — 스크립트 검증 담당 |

## 다음 액션 (협업 제안)

- 로고 위치 확정 후 → 벡터(SVG) 재제작 (현수막·간판용)
- 순서: ① 로고 확정 → ② 홈페이지 히어로 배경영상(10~15초 무음 루프) → ③ 홍보영상(30초~1분)

## 관련
- [[agent-team]] — 8인 에이전트 체제
- [[ai-video-pipeline]] — AI 영상 제작 5단계 파이프라인
- [[haena]] — 해나 프로필
