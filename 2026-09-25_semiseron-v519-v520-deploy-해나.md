# semiseron.kr 배포 기록 — v5.1.9 / v5.2.0 (2026-09-25)

> **배포한 사람**: 해나 (Hermes)
> **지금까지 배포자**: 에이지 (AntiGravity 2.0) — 9/11 V13 릴리즈, 9/14 정식 리뉴얼, 9/22 사업자등록 푸터
> **오늘은 해나가 올렸습니다.** 이 문서는 에이지가 이어받을 때 보라고 남깁니다.
> **마스터님 승인**: 2026-09-25 "실수하지 말고 잘하자. 올려."

---

## 1. 오늘 올린 것 (4건, 전부 라이브 반영)

| 커밋 | 내용 | 바뀐 파일 | 라이브 반영 |
|:----:|------|:--------:|:----------:|
| `7c5397f` | 상단 메뉴 깨짐 수정 + 서랍 정리 | index.html 1개 | 00:33 이전 |
| `fb89d4d` | 기록물 링크 2건 복구 + 종이 배경 도입 | 9개 | 00:33:24 |
| `6836dfa` | 검은 박스 여백 결함 + 취약 CSS 수정 | index.html 1개 | 00:57:00 |
| `93e7ee9` | 글씨 읽힘 보강 + 로고 원복 + 이력 창 v5.1.9/v5.2.0 | index.html 1개 | **02:04:44** |

저장소: `TedChang-Lim/meta-ai-labs` · 브랜치 `main` · GitHub Pages 자동 배포.
되돌리기: `git revert <sha>` (각 커밋이 독립적이라 하나씩 되돌릴 수 있음).

---

## 2. 무엇을 어떻게 바꿨나

### 2.0 읽힘 보강 (마스터님 지적, 커밋 `93e7ee9`)

| 잰 곳 | 전 | 후 |
|-------|:--:|:--:|
| 수료 안내 칩 글씨 | 11px · 굵기 300 · #8A6420 | **12px · 굵기 500 · #2E2A22** |
| 칩 강조(수료 후 남는 것:) | 굵기 400 | **굵기 700** |
| 칩 실제 렌더 대비 | 4.92:1 | **11.61:1** |
| MASTER TRACKS 줄 | 11px · 자간 0.2em | **13px · 자간 0.16em** |
| 워드마크 로고 | 38px | **44px** (캠퍼스·훈련원과 같게) |

칩은 `[style*=...]` 글자 일치에 기대지 않고 **이름표(`class="earn-chip"`)로 색·크기를 못 박았습니다.**
인라인 스타일 값은 한 글자도 손대지 않았습니다 — 3.1 의 결함 재발 방지.

### 2.1 상단 메뉴가 좁은 화면에서 세로로 깨지던 것

메뉴가 필요로 하는 폭이 **1434px** 인데 화면이 그보다 좁으면 버튼이 줄바꿈돼 세로로 늘어졌습니다.

| 손댄 곳 | 전 | 후 |
|---------|:--:|:--:|
| 모닝샘 AI 브리핑 단추 | 있음 | 삭제 (왼쪽 '데일리 모닝샘'과 중복) |
| ⌨️ 아이콘 | 상단·서랍 | 삭제 (본문 배지는 보존) |
| 단추 여백 / 자간 | 18px / 0.2em | 13px / 0.1em |
| 메뉴 간격 / 자간 | 28px / 0.18em | 9px / 0.07em |
| 로고 높이 | 44px | **38px** ← 이것 때문에 나중에 문제 |
| 서랍 전환 기준 | (없음) | `@media (max-width: 1450px)` |
| 서랍 항목 | — | CLASSROOM & ARCHIVE 추가 |

결과: 필요 폭 **1434 → 1267px** (-167px). 1512px 에서 여유 109px.

### 2.2 끊겨 있던 기록물 링크 2건

| 가던 곳 | 실제 | 고친 곳 |
|---------|------|---------|
| `proposal/KACEC_proposal.html` (404) | 파일이 저장소 밖에 있었음 | `proposal/semiseron-proposal-2026.html` |
| `saemisaeron_homepage_v1.html` (404) | 없던 파일 | 복구 |

서랍의 `AI LABS` → `#lab`(빈 곳), `CERTIFICATIONS` → `campus.html#certifications`(index 에 그 구간 없음).
`(PDF)` 표기 2곳 → `(HTML)`.

### 2.3 종이색 배경 (K 시안 light-band)

K(`semiseron_v13/index.html`)의 종이 배경을 **통째로 올리지 않고 부분 이식**했습니다.
통째로 올리면 사업자등록 푸터와 9월 업데이트가 사라지기 때문입니다.

- CSS 21줄 + 클래스 4곳만 이식.
- **검게 남아야 하는 박스 2곳**(EDUCATION 안내 박스 2개)은 그대로 둠 — 종이 위에서 글이 묻히지 않게.
- EDUCATION / CLASSROOM 배경 `rgb(236,232,223)`.

---

## 3. 오늘 잡은 결함 2건 (에이지가 같은 실수 안 하게)

### 3.1 검은 박스 여백이 0 이 됐던 것

`class` 를 끼워 넣으면서 `style` 값을 **다시 조립**했더니 배경 선언이 겹쳐 들어가
`padding`·`border`·`margin` 이 통째로 무효가 됐습니다 (마스터님 지적).

> **규칙**: 기존 요소에 클래스만 끼울 때는 `style` 값을 **손대지 마십시오.** 값이 필요하면 따로 붙이십시오.

### 3.2 카드가 투명해져 흰 글씨가 종이에 묻힌 것

종이 배경 규칙을 `[style*="background:rgba(255,255,255,0.02)"]` 처럼 **글자 그대로 일치**에 기대어 만들었더니,
그 문구가 한 글자라도 어긋나면 규칙이 안 걸려 카드가 투명해졌습니다.

> **규칙**: `[style*=...]` 로 색을 잡지 마십시오. **클래스 기준**으로 잡으십시오.
> 안전망: `.light-band .track-card, .light-band .card-box { background:#ffffff !important; }`

### 3.3 `.light-band .section-title-sub` 가 글자를 지운 것

이 규칙이 글자를 `opacity: 0` 으로 만들고 그라데이션으로 채우는 방식이라,
Chrome 에서 EDUCATION 제목 덩어리가 통째로 사라졌습니다. 취약 규칙 제거.

### 3.4 `훈련원` 이 `훈` / `련원` 으로 끊긴 것

사이트 전체가 `word-break: keep-all` 인데 그 제목만 빠져 있었습니다.

---

## 4. 검증 방식 (숫자로)

**`vision_analyze`(눈으로 보는 검사)는 판정 근거로 쓰지 마십시오.**
같은 그림에 "안 보이는 글자 0건" 과 "제목 5개 전부 안 읽힘" 이라는 상반된 답을 냈습니다.

- Chrome `channel:'chrome'` 으로 마스터님 화면 폭(≈1433px)에서 측정.
- 실제 렌더 픽셀(PIL)로 대비를 계산. 기준 3:1.

| 잰 곳 | 값 |
|-------|:--:|
| 트랙 카드 제목 | 17.61:1 |
| 트랙 카드 본문 | 8.15:1 |
| 수료 안내 칩 | 4.92:1 |
| 종이 위 작은 제목 | 6.26:1 |

---

## 5. 일괄 수정 스크립트 (이어받을 때 쓰십시오)

`~/.hermes/scripts/apply-site-fixes.py`

- 기본은 **모의실행**(`.fixpreview.html`), `--apply` 로 실반영.
- **멱등** — 두 번 돌려도 결과가 같습니다.
- **`SITE_REPO` 환경변수** 지원 — 반드시 임시 사본을 가리켜 시험하십시오.
  (초기 버전은 `REPO` 가 절대경로로 박혀 있어, 사본에서 시험한 줄 알았는데 실제 저장소를 고쳤습니다.)

```bash
SITE_REPO=/tmp/사본 python3 ~/.hermes/scripts/apply-site-fixes.py --apply
```

---

## 6. 남은 것

- **AI BASIC 카드 안쪽 글씨 색이 브라우저마다 달랐던 건** — 원인은 칩 색이 `[style*=...]` 글자 일치에
  기대고 있었던 것. `93e7ee9` 에서 이름표 방식으로 바꿔 더 이상 그 구조에 기대지 않습니다.
  (해나 Chrome 실측 1500·1536·1600·1680·1920·1440·1433px 에서는 재현이 안 됐으나, 구조 자체를 없앴습니다.)
- **`kacec.kr` 대표 주소 404** — 점유 저장소 없음. 복구 미완.
- **`index.html` 에 자격증 구간 없음** — `campus.html#certifications` 로 우회 중.
- **자격증 팝업 단추 0개** — `certsModalData` 5종과 `openCertModal` 함수만 있고 부르는 곳이 없음.
- **Safari 인트로 애니메이션 미작동** — WebKit 재현 실패(5/5 정상).

---

## 7. 지켜야 할 규칙 (오늘 어긴 것 포함)

1. 원본은 한 글자도 건드리지 않고 **임시 사본으로 먼저 시험** → 화면을 보여드림 → "올려" 승인 후 push.
2. **승인은 보여드린 화면과 똑같을 때만 유효.** 다른 색·글자로 올리면 승인 위반.
3. 검사에 Chrome 을 쓰면 **반드시 닫으십시오.** 안 닫으면 프로세스가 남아 마스터님이 크롬을 눌러도 창이 안 뜹니다.
4. 커밋 본문 맨 아래에 `작업: 해나` 한 줄 (모든 에이전트가 같은 git 이름으로 커밋돼 구분이 안 되기 때문).

---

*작업: 해나 · 2026-09-25*
